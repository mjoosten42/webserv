Servers and clients pollfds are stored in a vector.
If server has POLLIN, accept.
If client has POLLIN, handle request.
Map of <fd, request>
Servers in vector, from config

Headers headers;
headers.statusline = "200 OK";
headers["Connection"] = "Keep-Alive";
headers["Content-Type"] = "text/html";
headers.setBodySize(500);
headers.fromString("derp");

headers.buildString();
